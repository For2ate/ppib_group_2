---
- name: Deploy Monitoring Stack (Prometheus)
  hosts: monitoring-servers
  become: true
  
  vars_files:
    - "../../vars/global.yml"
    - "../../secrets/vault_secrets.yaml"

  roles:
    - role: deploy-prometheus